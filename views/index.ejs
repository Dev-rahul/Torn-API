<meta http-equiv="refresh" content="10"/>
<% for(var i = 0; i < data.length; i++) { %>
  <% for(var j = 0; j < data[i].length; j++) { %>
    <h2><%= data[i][j].name %> is available</h2>
  <% } %>
  <% } %>